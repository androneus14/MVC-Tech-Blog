<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <!-- stylesheets to be added -->
</head>

<body>
    <div class="header-container">
        <header class="hero">
            <div>
                <nav class="navigation-bar">
                    <a class="navbar-link" href="/">Home</a>
                    <a class="navbar-link" href="/dashboard">Dashboard</a>
                    {{#if logged_in}}
                    <a class="navbar-link" href="/" id="logout-btn">Logout</a>
                    {{else}}
                    <a class="navbar-link" href="/login">Login</a>
                    {{/if}}
                </nav>
            </div>

            <h1 class="tech-blog-title"><a href="/">Coding Tech Blog</a></h1>
        </header>

        <main>
            {{{ body }}}
        </main>
    </div>

    {{#if logged_in}}
    <!-- js link to be added -->
    {{/if}}

</body>

</html>